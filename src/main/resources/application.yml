micronaut:
  application:
    name: admin
  metrics:
    export:
      prometheus:
        enabled: true
        descriptions: true
        step: PT1M
    enabled: true
cassandra:
  default:
    contactPoint: '"localhost"'
    port: 9042
kafka.bootstrap.servers: localhost:9092
